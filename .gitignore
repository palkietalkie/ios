# Xcode project + sibling artifacts (all regenerated by xcodegen from project.yml; Info.plist also embeds env-interpolated values that change per machine)
PalkieTalkie.xcodeproj
PalkieTalkie/PalkieTalkie.entitlements
PalkieTalkie/Info.plist

# Xcode build artifacts
build/
DerivedData/
*.xcuserdata

# Test result bundles (from `xcodebuild test -resultBundlePath`; thousands of internal files VSCode's watcher chokes on)
TestResults
*.xcresult

# SPM dependency cache
.build/
.swiftpm/

# sourcekit-lsp ↔ Xcode bridge (xcode-build-server). Absolute paths, machine-local.
buildServer.json

# macOS
.DS_Store
*.swp

# Claude Code (personal local overrides)
.claude/settings.local.json

# Secrets / API tokens
.env
.env.local
.env.production

.pr-bodies/
